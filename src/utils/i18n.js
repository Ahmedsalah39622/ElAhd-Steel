// src/utils/i18n.js

// Placeholder for getLocalizedUrl
export function getLocalizedUrl(url, locale = 'en') {
  // This is a simple placeholder implementation
  // Replace with your actual localization logic
  return `/${locale}${url}`
}
